[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "csm"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["numpy", "matplotlib", "pillow", "tqdm", "pyyaml"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["csm*"]
