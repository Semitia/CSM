[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "csm"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["numpy", "matplotlib", "tqdm", "pyyaml"]

[tool.setuptools.packages.find]
where = ["."]
include = ["scripts*"]
