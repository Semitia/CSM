我的matlab是2024b，最好参考一下官方最新文档
参考论文效果图(image_article)。我要和论文一样的效果。半透明的平滑的，而且能支持凹下去的形状，作为translation workspace，然后无法达到的地方就用另一个颜色填充，但也不是所有不可达的抵挡都画了出来。我猜测他可能是用了一个凸包包裹了translation workspace，凸包减去translation workspace就是这个不可达区域。

目前plot_workspace_2.py有效果图，你可以看assets/。问题是没有画出不可抵达的地方。
另外，有不平滑的问题，总感觉坑坑洼洼的，我都怀疑是不是实现的大方向搞错了？因为这看起来就是很简单的一张图！

我目前的实现方案应该确实偏了。目前表面会出现丝带状的条纹，这肯定不行。我之前的思路是用gen_workspace生成点云，然后画图。但是我现在想直接在plot里面全部实现。具体而言，一样是加载机器人和配置。但是采样的时候，phi\delta1\delta2全都不用管了，只均匀遍历theta和l，这样点的数量级会大大下降，我可以增加密度。然后做轮廓提取，并用直线/平滑曲线拟合，包括可达区间和不可达区间。最后直接把轮廓旋转。

我发现一个问题。原文那个看起来不像是透明立体图啊？刚刚才发现。他像是被压扁在平面的图，没有立体感的，也没有半透明效果（你把是绿色棕色的区域和上面的淡蓝色方盒比较一下就能看出来），但是他就是能让内部的不可见区域的图层在上面。这是什么画法？

你可以看最新的效果。几个问题。1，side view的3d版本，视角不对，不是侧视；而主视图是斜视的视角，但其伪3d怎么变成侧视了？2，side view的伪3d就不要了，因为和flat 2d不是一样的效果吗？统一叫伪3d好了，和main一样。3.为啥现在只显示mode3的图？4.把采样的2d散点图也画一下，方便检查。

目前最右下角和中间圆弧的最顶部还有缺口，是什么问题？
实在不行我在想能否用类似cad软件里面那种“延长”操作？

mode2,3,4看起来没太大问题。mode1还有问题，表现为：1.顶端没补齐；2.外轮廓有锯齿；3.最下面其实是一段完整的圆弧（从这个构型的曲率限制就能知道），直接连到上面一条曲线。现在是半路就用直线连接了。可能不是简单的bug，可以参考其他mode实现方式，深入分析。难道不应该是要比mode3简单不少嘛？为什么反而一直效果不好？同一条水平线从内向外发射可能会经过两次mode1的区域？因为别的mode都是倒扣碗状的，mode1是圣诞树形状的，其实没有严格的“内部的不可达区域”，所以要对mode1单独写一份，其实也很简单。直接全部都是外轮廓不就行了？

轮廓没什么问题了，但是为什么会有锯齿？别的mode都是平滑的曲线。我记得是有用曲线拟合这一步的？另外，mode1，mode2的采样数量可以加大，本身其config space 维度就比较低，多一点也没关系。

现在只剩下小尖尖问题了。其实应该很简单。我们已经有了圆弧helper(不知道目前怎么实现的），或者跟这个压根没关系，那我们新建一个helper对象，但我觉得就是对曲线末端取一小段，拟合出半径和圆心，选取曲线的末端就是我们生成的圆弧的起点，那就只剩下一个未知量，就是弧度（弧长），有这个可以算出终点位置。另一个线如果是直线就很好算了，如果也是曲线，也用这个helper，那就是两个未知量，然后末端共点，也就是交点，x相等，y相等，有两个等式，求方程组就能求出来。当然可能不止一个解，应该选取能使弧度最小的那个。mode1,3应该是两个曲线，mode2,4就是1个直线+1曲线，只不过现在mode2的内轮廓末端长出来一段直线，不知道是什么问题导致的。


接下来接一个功能。mode 0就是直接绘制所有的mode，各个mode的点拼合为整体，在对这个整体提取内外轮廓，可视化，main和side view也是只有可行和不可行两个颜色，不过散点图里面的绘制还是要用不同颜色区分不同mode的可行区域。

目前mode 0的绘制还有点问题。大体意思对了，但是还差点准确度。最后少了一段直线，而且边似乎贴的不够紧？其实这个整体的内轮廓，就是由内到外分别是mode1的下圆弧，mode2外轮廓直线的一部分，一部分mode3内轮廓，最后是mode4内直线段。另外最上方的圆弧怎么没闭合？你可以详细参考其他mode的绘制方法。conda csm是环境。

- [ ] 
现在可视化加载缓慢，而且加载出来后界面拖动卡顿。我在想点多只是因为计算需要，可视化完全没有必要显示太多吧，在不影响视觉效果的情况下，应该可以大大优化？

- [x] 
最小弯曲半径
现在的config文件是给了最小弯曲半径的，但是我怀疑目前的csm在加载配置的时候没有读取并使用。

- [x]
mode 0绘制有bug
目前的mode 0的可视化还有bug。这里第一张图对应csm_cfg_3mm的配置，内轮廓识别有问题，第二张是csm_cfg_0的配置，没有问题。我现在在考虑能否让mode 0 的绘制逻辑更稳健一点，能适应不用cfg

- [x] 
plot_workspace_2 添加arm可视化
我现在有csm.plot_manipulator(ax, render_mode="detailed")方法，我想让plot_workspace_2现在在绘制每个mode的时候顺便采样一个对应mode的配置，然后画一个机械臂上去。

- [x] mode1 内轮廓
plot_workspace_2 现在是mode2，3，4都有内轮廓/内部不可达域的绘制的，但是mode1没有。我现在想了一下还是应该有。思路和其他mode类似，也是一个凸包包裹了translation workspace，凸包减去translation workspace就是这个不可达区域。两张图片，一个是目前效果，一个是我想要的效果（我先用手画的，你应该能理解我的意思？）
- [ ] 还有bug
第一张图是3mm配置，第二张是6mm，我换了一个配置文件，mode 1 的绘制就出问题了

- [ ] 
3.4mm,6mm

- [ ] 
主要分两个view，一个是main，斜视；另一个是side，侧视图。现在main的real_3d和side的pseudo_3d没啥问题。但是main的pesu和side的real有问题。
补充一下关于pesudo_3d，可参考assets/image_article.png，大致就是说不是透明立体图，像是被压扁在平面的图，没有立体感的，也没有半透明效果（你把绿色棕色的区域和上面的淡蓝色方盒比较一下就能看出来），但是他就是能让内部的不可见区域的图层在上面。而且是斜视的视角，不是普通的某个坐标轴方向的正视，像是“三渲二”，我不知道这样形容是否合适。arm的图层应该在最上面，translation space应该图层在最下面。unreachable的在中间？


- [x] side view arm
plot_workspace_2现在在绘制每个mode的时候顺便采样一个对应mode的配置，然后画一个机械臂上去。但是现在只在main view画了，我希望side view也有
- [ ] detailed

- [x]
在ref/cosserat_chen里面有matlab代码，我想参考其中的机器人可视化相关的脚本，为我的csm/可视化添加类似的功能，不过他可能是用的cosserat建模，但是我们用的是圆弧近似。我原来是用一根线代替的，新功能要丰富一点，backbone圆周排列的驱动丝，沿弧排列的间隔盘，末端工具等，他代码里应该有，assets/下的image或可参考。

- [ ] 通用绘制
plot_workspace_2 现在的问题是实在不稳健，我的csm配置文件稍微换一个轮廓提取拟合求交就会出大问题。我觉得需要一个更优雅更通用泛化的方案。甚至需要大改动。你先别动手，帮我想想具体的可行方案。